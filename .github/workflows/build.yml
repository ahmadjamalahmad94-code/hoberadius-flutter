name: build-apk

# Builds an installable release APK for the HobeRadius admin app.
#
# The repo intentionally has no committed `android/` folder (it is gitignored;
# this is a web/desktop-first Flutter project). CI scaffolds the Android
# platform with `flutter create --platforms=android .`, then builds a release
# APK signed with the standard debug keystore (the Flutter android template
# signs `release` with `signingConfigs.debug`), so the artifact installs
# directly without a production signing setup.

on:
  push:
    branches: [ci/android-apk]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          # Match the dev machine's Flutter (intl 0.20.2 needs >=3.29; older
          # SDKs pin intl 0.19.0 via flutter_localizations and fail to resolve).
          flutter-version: "3.44.1"
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Test (excluding platform-specific golden/render tests)
        run: flutter test --exclude-tags "golden || render"

      - name: Scaffold Android platform (android/ is gitignored)
        run: flutter create --platforms=android --org com.hoberadius .

      - name: Generate debug keystore (release is debug-signed → installable)
        run: |
          mkdir -p ~/.android
          if [ ! -f ~/.android/debug.keystore ]; then
            keytool -genkeypair -v \
              -keystore ~/.android/debug.keystore \
              -storepass android -keypass android \
              -alias androiddebugkey \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi

      - name: Patch stale-plugin Android compileSdk
        # desktop_drop 0.4.4 (desktop-only drag-drop) hardcodes compileSdk 33,
        # but its transitive androidx deps require consumers compiled against
        # 34+. The plugin has no use on Android; bump its compileSdk so the
        # AAR-metadata check passes. CI regenerates the pub cache each run, so
        # this never touches the committed source.
        run: |
          CACHE="${PUB_CACHE:-$HOME/.pub-cache}"
          found=0
          for g in $(find "$CACHE" -path "*desktop_drop*/android/build.gradle" 2>/dev/null); do
            echo "patching $g"
            sed -i -E 's/compileSdk(Version)?[[:space:]]+33/compileSdk 35/' "$g"
            grep -nE "compileSdk" "$g" || true
            found=1
          done
          [ "$found" = "1" ] || echo "no desktop_drop gradle found (ok if removed)"

      - name: Pre-install Android CMake 3.22.1
        # A transitive plugin (jni) builds native code via CMake 3.22.1. Letting
        # the Gradle build auto-download it flaked ("Error reading Zip content");
        # install it up front via sdkmanager with retries so the build is stable.
        run: |
          SDK="${ANDROID_SDK_ROOT:-$ANDROID_HOME}"
          SDKMGR="$SDK/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMGR" --licenses >/dev/null 2>&1 || true
          ok=0
          for i in 1 2 3; do
            if "$SDKMGR" "cmake;3.22.1"; then ok=1; break; fi
            echo "cmake install attempt $i failed, retrying…"; sleep 10
          done
          [ "$ok" = "1" ] || { echo "::error::failed to install cmake;3.22.1"; exit 1; }
          ls -d "$SDK/cmake/3.22.1" && echo "cmake 3.22.1 ready"

      - name: Build release APK
        run: flutter build apk --release

      - name: Rename APK
        run: |
          mkdir -p artifacts
          cp build/app/outputs/flutter-apk/app-release.apk artifacts/radius-app.apk
          ls -l artifacts

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: radius-app-apk
          path: artifacts/radius-app.apk
          if-no-files-found: error
          retention-days: 14
